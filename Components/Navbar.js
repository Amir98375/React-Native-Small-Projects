import React from 'react'
import { ScrollView, Text, View } from 'react-native'

export const Navbar = () => {
  return (
    <ScrollView>
      <View >
      <Text style={{alignContent:'center'}}>
        first react native reusable component
      </Text>
      </View>
    </ScrollView>
  )
}
